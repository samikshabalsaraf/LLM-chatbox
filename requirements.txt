# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# HTTP client for Ollama / Hugging Face API calls
httpx>=0.26.0

# HTML templates (Jinja2 comes with FastAPI)
jinja2>=3.1.2

# Optional: for loading .env (Hugging Face token, etc.)
python-dotenv>=1.0.0
